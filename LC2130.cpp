class Solution {
public:
    int pairSum(ListNode* head) {
        stack<int> s;

        ListNode* mover = head;
        bool ok = 0;
        int ans = INT_MIN;
        int n = 0;
        while(mover) {n++; mover = mover->next;}

        
    }
};